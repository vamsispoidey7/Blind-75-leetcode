# 15. 3Sum

**Problem Link:** https://leetcode.com/problems/3sum/

---

## 🖼️ Editorial / Visual Explanation

https://www.youtube.com/watch?v=DhFh8Kw7ymk
Striver Video is better than leetcode editorial for this

---

## Brute Force Approach

### 💻 C++ Code
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        set<vector<int>> s;
        int n = nums.size();
        for(int i=0;i<n;i++){
            for(int j=i+1;j<n;j++){
                for(int k=j+1;k<n;k++){
                    if(nums[i]+nums[j]+nums[k]==0){
                        vector<int> temp = {nums[i],nums[j],nums[k]};
                        sort(temp.begin(),temp.end());
                        s.insert(temp);
                    }
                }
            }
        }
        vector<vector<int>> result(s.begin(),s.end());
        return result;
    }
};
```

### ⏱ Complexity
- **Time Complexity:** `O(N^3 * log M)` M is number of triplets
- **Space Complexity:** `O(M)`

---

## Better Approach

### 💻 C++ Code
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        set<vector<int>> s;
        int n = nums.size();
        for(int i=0;i<n;i++){
            set<int> hash;
            for(int j=i+1;j<n;j++){
                int target = -(nums[i]+nums[j]);
                if(hash.find(target)!=hash.end()){
                    vector<int> temp = {nums[i],nums[j],target};
                    sort(temp.begin(),temp.end());
                    s.insert(temp);
                }
                hash.insert(nums[j]);
            }
        }
        vector<vector<int>> result(s.begin(),s.end());
        return result;
    }
};
```

### ⏱ Complexity
- **Time Complexity:** `O(N^2 * log M)`
- **Space Complexity:** `O(N)`

---

## Optimal Approach

### 💻 C++ Code (Overflow-Safe)
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result;
        int n = nums.size();
        sort(nums.begin(),nums.end());
        for(int i=0;i<n;i++){
            if(i>0 && nums[i]==nums[i-1]) continue;
            int low = i+1, high = n-1;
            while(low<high){
                int sum = nums[i] + nums[low] + nums[high];
                if(sum==0){
                    result.push_back({nums[i],nums[low],nums[high]});
                    low++;
                    high--;
                    while(low<high && nums[low]==nums[low-1]) low++;
                    while(low<high && nums[high]==nums[high+1]) high--;
                }else if(sum<0){
                    low++;
                }else{
                    high--;
                }
            }
        }
        return result;
    }
};
```

### ⏱ Complexity
- **Time Complexity:** `O(N^2)`
- **Space Complexity:** `O(1)`

---
